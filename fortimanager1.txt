config system ha
    set failover-mode vrrp
    set mode primary
        config peer
            edit 1
                set ip 10.2.1.10
                set serial-number "FMG-VMTM25014033"
            next
        end
    set password ClusterPassword
    set priority 200
    set unicast enable
    set vip "192.0.2.1"
    set vrrp-interface "port1"
end
